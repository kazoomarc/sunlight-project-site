# after This

    $$\sin (3\theta + \phi) + \sin (\theta + 3\phi) = 2 \sin(\theta + \phi)(\cos(2\theta) + \cos(2\theta))
    $$

    before faster formula do some python
    $$note: (3\theta = 2\theta + \theta)$$
    $$\sin(2\theta + (\theta + \phi)) + \sin(2\phi+(\theta + \phi))$$

    $$sin(2\theta)\cos(\theta + \phi) + \cos(2\theta)\sin(\theta + \phi)+\sin(2\theta)\cos(\theta + \phi) + \cos(2\theta)\sin(\theta + \phi)$$

    $$[\cos(\theta + \phi)(\sin(2\theta) + \sin(2\phi))] + \sin(\theta + \phi)(\cos(2\theta) + \sin(2\theta))$$

    <center>prove</center>
    $$[\cos(\theta + \phi)\sin(2\theta) + \cos(\theta + \phi)\sin(2\theta)] = \sin(\theta + \phi)(\cos(2\theta + \sin(2\theta)))$$

    <center>=====</center>

    $$\frac{1}{2}(\sin(\theta+ \phi + 2\theta) - \sin(\theta + \phi - 2\theta)) = \frac{1}{2}(\sin(3\theta+ \phi ) -\sin( \phi - \theta))$$

    <center>=====</center>
    $$\frac{1}{2}(\sin(\theta+ \phi + 2\phi) - \sin(\theta + \phi - 2\phi)) = \frac{1}{2}(\sin(3\phi+ \theta ) - \sin(\phi - \theta))$$

    $$\frac{1}{2}(\sin(3\theta + \phi) + \sin(3\phi + \theta))$$
    <center>#####</center>

    {/* $$\sin(\frac{3\theta + \phi + 3\phi + \theta }{2}) \cos(\frac{3\theta + \phi - 3\theta - \theta}{2})$$ */}

    $$\sin(2\theta + 2\phi) \cos(\theta - \phi)$$

    $$2 \sin(\theta + \phi) \cos(\theta + \phi) \cos(\theta - \phi)$$

    $$2\sin(\theta + \phi)(\frac{1}{2}(\cos(\theta + \phi + \theta - \phi) + \cos(\theta + \phi - \theta + \phi ))---??$$

    $$\sin(\theta + \phi)(\cos(2\theta) + \cos(2\theta))$$

    <center>May have been easier just to use faster formula</center>
